<h1>Add a review</h1>
<br>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.label 'rating' %>
  <%= f.number_field :rating %>

  <%= f.label 'content' %>
  <%= f.text_field :content %>

  <%= f.submit "create the review", class: "btn btn-primary" %>
<% end %>
